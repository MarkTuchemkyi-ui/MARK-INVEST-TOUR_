# ⚡ Быстрый деплой на Railway

Краткая инструкция для быстрого развертывания проекта на Railway.com.

## 📋 Чеклист (5 минут)

### 1. GitHub (2 минуты)

```bash
# Если Git еще не инициализирован
git init
git add .
git commit -m "Initial commit"

# Создайте репозиторий на GitHub.com, затем:
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
git branch -M main
git push -u origin main
```

### 2. Railway (2 минуты)

1. Откройте [railway.com](https://railway.com) → Войдите через GitHub
2. **New Project** → **Deploy from GitHub repo** → Выберите ваш репозиторий
3. Railway автоматически начнет деплой

### 3. Настройка переменных (1 минута)

В Railway: **Settings** → **Variables** → Добавьте:

```env
JWT_SECRET=ваш-секретный-ключ-минимум-32-символа-для-безопасности
NODE_ENV=production
```

**Готово!** 🎉 Приложение должно запуститься.

## 🔧 Дополнительная настройка (опционально)

### Email уведомления

```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ваш-email@gmail.com
SMTP_PASS=пароль-приложения-gmail
SITE_URL=https://ваш-проект.railway.app
```

### Постоянное хранилище для БД

1. В Railway: **+ New** → **Volume**
2. Создайте volume с именем `database`
3. Railway автоматически подключит его

### Домен

1. **Settings** → **Domains** → **Generate Domain**
2. Обновите `SITE_URL` с новым доменом

## 📚 Подробные инструкции

- **Полная инструкция**: `RAILWAY_DEPLOY.md`
- **Настройка GitHub**: `GITHUB_SETUP.md`
- **Переменные окружения**: `ENV_VARIABLES.md`

## ✅ Проверка

После деплоя проверьте:

- ✅ Главная: `https://ваш-проект.railway.app/`
- ✅ Админ: `https://ваш-проект.railway.app/admin` (admin/admin123)
- ✅ Health: `https://ваш-проект.railway.app/api/health`

---

**Время деплоя**: ~5 минут ⚡

