# 📦 Настройка Railway Volume для постоянного хранения базы данных

Это руководство поможет вам настроить Railway Volume для сохранения данных SQLite базы данных даже после редеплоя.

## 🎯 Зачем это нужно?

По умолчанию Railway использует **эфемерную файловую систему** - это означает, что все файлы, созданные во время работы приложения, **удаляются при каждом редеплое**. 

Для SQLite базы данных это критично, так как все ваши данные (туры, заявки, подписки) будут потеряны при каждом обновлении приложения.

**Railway Volume** - это постоянное хранилище, которое сохраняет данные между деплоями.

---

## 📋 Пошаговая инструкция

### Шаг 1: Создание Volume на Railway

1. **Откройте ваш проект на Railway.com**
   - Перейдите на [railway.app](https://railway.app)
   - Войдите в свой аккаунт
   - Выберите ваш проект `MARK-INVEST-TOUR`

2. **Создайте новый Volume**
   - В панели проекта нажмите кнопку **"+ New"** (в правом верхнем углу)
   - Выберите **"Volume"** из списка
   - Введите имя: `database-volume` (или любое другое)
   - Нажмите **"Add"**

3. **Подключите Volume к сервису**
   - После создания Volume, Railway покажет его в списке сервисов
   - Нажмите на созданный Volume
   - В разделе **"Connect"** выберите ваш сервис (обычно это сервис с вашим приложением)
   - Нажмите **"Connect"**

4. **Проверьте переменную окружения**
   - Railway автоматически создаст переменную `RAILWAY_VOLUME_MOUNT_PATH`
   - Перейдите в **Settings** → **Variables** вашего сервиса
   - Убедитесь, что переменная `RAILWAY_VOLUME_MOUNT_PATH` присутствует
   - Значение будет выглядеть примерно так: `/mnt/volume/...`

### Шаг 2: Проверка конфигурации приложения

Ваше приложение уже настроено для работы с Railway Volume! 

Код в `server/config/database.js` автоматически:
- ✅ Проверяет наличие переменной `RAILWAY_VOLUME_MOUNT_PATH`
- ✅ Использует Volume для хранения `travel.db`
- ✅ Создает директорию, если её нет
- ✅ Логирует путь к базе данных при запуске

### Шаг 3: Проверка работы

1. **Запустите редеплой**
   - После подключения Volume, Railway автоматически перезапустит сервис
   - Или нажмите **"Redeploy"** в разделе Deployments

2. **Проверьте логи**
   - Перейдите в **Deployments** → выберите последний деплой
   - Откройте **Logs**
   - Найдите строку: `✅ Обнаружен Railway Volume, используем путь: /mnt/volume/.../travel.db`
   - Если видите эту строку - всё работает правильно! ✅

3. **Проверьте сохранение данных**
   - Войдите в админ-панель: `https://ваш-проект.railway.app/admin`
   - Создайте тестовый тур или заявку
   - Выполните редеплой (нажмите **"Redeploy"**)
   - Проверьте, что данные сохранились после редеплоя

---

## 🔧 Альтернативный способ: Ручная настройка пути

Если по какой-то причине автоматическое определение не работает, вы можете указать путь вручную:

1. **Узнайте путь к Volume**
   - В настройках Volume найдите путь монтирования
   - Обычно это что-то вроде `/mnt/volume/...`

2. **Добавьте переменную окружения**
   - Перейдите в **Settings** → **Variables** вашего сервиса
   - Добавьте новую переменную:
     ```
     DB_PATH=/mnt/volume/ваш-volume-id/travel.db
     ```
   - Замените `ваш-volume-id` на реальный ID вашего Volume

3. **Приоритет переменных**
   - Приложение использует переменные в следующем порядке:
     1. `DB_PATH` (если указана вручную)
     2. `RAILWAY_VOLUME_MOUNT_PATH` (автоматически создается Railway)
     3. Локальный путь `travel.db` (только для разработки)

---

## 🐛 Решение проблем

### Проблема: Данные всё равно теряются после редеплоя

**Возможные причины:**
1. Volume не подключен к сервису
   - **Решение:** Проверьте, что Volume подключен в разделе "Connect"

2. Переменная `RAILWAY_VOLUME_MOUNT_PATH` отсутствует
   - **Решение:** Переподключите Volume к сервису

3. Приложение использует локальный путь
   - **Решение:** Проверьте логи при запуске. Если видите `⚠️ Используется локальный путь`, значит Volume не обнаружен

### Проблема: Ошибка "Permission denied" при записи в БД

**Решение:**
- Railway Volume должен иметь правильные права доступа
- Если проблема сохраняется, попробуйте пересоздать Volume

### Проблема: База данных не создается

**Решение:**
1. Проверьте логи приложения на наличие ошибок
2. Убедитесь, что путь к Volume существует
3. Проверьте, что у приложения есть права на запись в директорию Volume

---

## 📊 Мониторинг использования Volume

Railway предоставляет метрики использования Volume:

1. Перейдите в настройки Volume
2. В разделе **Metrics** вы увидите:
   - Используемое пространство
   - Доступное пространство
   - Процент использования

**Рекомендация:** Регулярно проверяйте использование Volume, чтобы не превысить лимиты.

---

## 💾 Резервное копирование

Хотя Railway Volume сохраняет данные между деплоями, рекомендуется делать резервные копии:

### Автоматическое резервное копирование (рекомендуется)

Вы можете настроить автоматическое резервное копирование через Railway или использовать внешний сервис.

### Ручное резервное копирование

1. Подключитесь к Railway через CLI или используйте API
2. Скопируйте файл `travel.db` из Volume
3. Сохраните его в безопасном месте

---

## ✅ Чеклист настройки

- [ ] Volume создан на Railway
- [ ] Volume подключен к сервису приложения
- [ ] Переменная `RAILWAY_VOLUME_MOUNT_PATH` присутствует в Variables
- [ ] Приложение успешно запустилось
- [ ] В логах видно: `✅ Обнаружен Railway Volume`
- [ ] Данные сохраняются после редеплоя
- [ ] Тестовые данные созданы и проверены

---

## 📚 Дополнительные ресурсы

- [Railway Volume Documentation](https://docs.railway.app/storage/volumes)
- [Railway Storage Best Practices](https://docs.railway.app/storage)

---

**Готово!** 🎉 Теперь ваша база данных будет сохраняться между деплоями!

