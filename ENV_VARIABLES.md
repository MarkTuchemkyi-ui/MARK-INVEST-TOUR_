# 🔐 Переменные окружения

Справочник по всем переменным окружения, используемым в проекте.

## Обязательные переменные

### `JWT_SECRET`
- **Описание**: Секретный ключ для подписи JWT токенов
- **Требования**: Минимум 32 символа
- **Пример**: `JWT_SECRET=my-super-secret-key-minimum-32-characters-long`
- **Где используется**: Аутентификация администраторов

### `NODE_ENV`
- **Описание**: Окружение выполнения приложения
- **Значения**: `development` | `production`
- **По умолчанию**: `development`
- **Пример**: `NODE_ENV=production`
- **Где используется**: Определяет режим работы приложения, логирование, обработка ошибок

## Переменные сервера

### `PORT`
- **Описание**: Порт, на котором запускается сервер
- **По умолчанию**: `3000`
- **Пример**: `PORT=3000`
- **Примечание**: Railway автоматически устанавливает эту переменную

## Переменные базы данных

### `DB_PATH`
- **Описание**: Путь к файлу базы данных SQLite
- **По умолчанию**: `travel.db` (в корне проекта)
- **Пример**: `DB_PATH=/data/travel.db`
- **Примечание**: 
  - Для Railway используйте Railway Volume
  - Railway автоматически предоставляет `RAILWAY_VOLUME_MOUNT_PATH`
  - Приложение автоматически использует Railway Volume, если он доступен

### `RAILWAY_VOLUME_MOUNT_PATH`
- **Описание**: Автоматически предоставляется Railway при создании Volume
- **Примечание**: Не нужно устанавливать вручную, Railway делает это автоматически

## Переменные Email (SMTP)

### `SMTP_HOST`
- **Описание**: Хост SMTP сервера
- **По умолчанию**: `smtp.gmail.com`
- **Пример**: `SMTP_HOST=smtp.gmail.com`
- **Альтернативы**: 
  - Gmail: `smtp.gmail.com`
  - Yandex: `smtp.yandex.ru`
  - Mail.ru: `smtp.mail.ru`

### `SMTP_PORT`
- **Описание**: Порт SMTP сервера
- **По умолчанию**: `587`
- **Пример**: `SMTP_PORT=587`
- **Альтернативы**: `465` (SSL), `25` (не рекомендуется)

### `SMTP_USER`
- **Описание**: Email адрес для отправки писем
- **Пример**: `SMTP_USER=your-email@gmail.com`
- **Требования**: Должен совпадать с адресом, для которого настроен SMTP

### `SMTP_PASS`
- **Описание**: Пароль для SMTP (для Gmail - пароль приложения)
- **Пример**: `SMTP_PASS=your-app-password`
- **Примечание**: 
  - Для Gmail нужно использовать пароль приложения, а не обычный пароль
  - Создать пароль приложения: https://myaccount.google.com/apppasswords

### `SITE_URL`
- **Описание**: Базовый URL сайта (используется в email уведомлениях)
- **По умолчанию**: `http://localhost:3000`
- **Пример**: `SITE_URL=https://your-app.railway.app`
- **Где используется**: Ссылки в email уведомлениях

## Переменные Redis (опционально)

### `REDIS_URL`
- **Описание**: URL подключения к Redis
- **По умолчанию**: `redis://localhost:6379`
- **Пример**: `REDIS_URL=redis://default:password@host:port`
- **Примечание**: 
  - Railway предоставляет Redis URL при добавлении Redis плагина
  - Формат: `redis://username:password@host:port`

### `REDIS_ENABLED`
- **Описание**: Включить/отключить Redis
- **Значения**: `true` | `false`
- **По умолчанию**: `true` (если Redis URL доступен)
- **Пример**: `REDIS_ENABLED=false`
- **Примечание**: Установите в `false`, если Redis недоступен

## Переменные CORS

### `CORS_ORIGIN`
- **Описание**: Разрешенные домены для CORS запросов
- **По умолчанию**: `*` (все домены)
- **Пример**: `CORS_ORIGIN=https://example.com,https://www.example.com`
- **Формат**: Домены через запятую без пробелов
- **Безопасность**: 
  - ⚠️ Не используйте `*` в production!
  - Указывайте конкретные домены
  - Используйте полные URL с протоколом (https://)

## Примеры конфигураций

### Локальная разработка

```env
PORT=3000
NODE_ENV=development
JWT_SECRET=development-secret-key-minimum-32-characters-long
SITE_URL=http://localhost:3000
CORS_ORIGIN=*
REDIS_ENABLED=false
```

### Production на Railway

```env
NODE_ENV=production
JWT_SECRET=production-secret-key-minimum-32-characters-long-and-secure
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SITE_URL=https://your-app.railway.app
CORS_ORIGIN=https://your-domain.com,https://www.your-domain.com
REDIS_URL=redis://default:password@host:port
```

## Проверка переменных

Приложение автоматически проверяет обязательные переменные при запуске:

- ✅ `JWT_SECRET` должен быть установлен и иметь минимум 32 символа
- ⚠️ Предупреждения выводятся для небезопасных настроек в production

## Где настроить на Railway

1. Откройте ваш проект на Railway
2. Перейдите в **Settings** → **Variables**
3. Добавьте переменные через интерфейс или импортируйте из файла
4. После изменения переменных Railway автоматически перезапустит приложение

## Безопасность

⚠️ **ВАЖНО:**

1. **Никогда не коммитьте `.env` файл в Git!**
2. Используйте надежные случайные ключи для `JWT_SECRET`
3. Не используйте `CORS_ORIGIN=*` в production
4. Храните секреты в переменных окружения Railway, а не в коде
5. Регулярно меняйте пароли и ключи

---

Для подробных инструкций по настройке смотрите `RAILWAY_DEPLOY.md`.

