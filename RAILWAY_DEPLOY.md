# 🚂 Деплой на Railway.com

Подробная инструкция по развертыванию проекта MARK INVEST TOUR на платформе Railway.com.

## 📋 Предварительные требования

1. Аккаунт на [Railway.com](https://railway.com)
2. Аккаунт на [GitHub.com](https://github.com)
3. Git установлен локально

## 🔧 Шаг 1: Создание репозитория на GitHub

### 1.1. Инициализация Git (если еще не инициализирован)

```bash
git init
git add .
git commit -m "Initial commit"
```

### 1.2. Создание репозитория на GitHub

1. Перейдите на [GitHub.com](https://github.com)
2. Нажмите кнопку **"New repository"** (или перейдите по ссылке https://github.com/new)
3. Заполните:
   - **Repository name**: `mark-invest-tour` (или любое другое имя)
   - **Description**: `MARK INVEST TOUR Website`
   - Выберите **Public** или **Private**
   - **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
4. Нажмите **"Create repository"**

### 1.3. Подключение локального репозитория к GitHub

```bash
# Замените YOUR_USERNAME и YOUR_REPO_NAME на ваши данные
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
git branch -M main
git push -u origin main
```

## 🚀 Шаг 2: Деплой на Railway

### 2.1. Создание проекта на Railway

1. Перейдите на [Railway.com](https://railway.com)
2. Войдите через GitHub
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите ваш репозиторий `mark-invest-tour`
6. Railway автоматически определит Node.js проект и начнет деплой

### 2.2. Настройка переменных окружения

После создания проекта, перейдите в **Settings** → **Variables** и добавьте следующие переменные:

#### Обязательные переменные:

```env
JWT_SECRET=ваш-секретный-ключ-минимум-32-символа-для-безопасности
NODE_ENV=production
```

**⚠️ ВАЖНО:** `JWT_SECRET` должен быть минимум 32 символа! Используйте надежный случайный ключ.

#### Переменные для Email (SMTP):

```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ваш-email@gmail.com
SMTP_PASS=ваш-пароль-приложения
SITE_URL=https://ваш-проект.railway.app
```

**Настройка Gmail:**
1. Включите двухфакторную аутентификацию в Google аккаунте
2. Создайте пароль приложения: https://myaccount.google.com/apppasswords
3. Используйте этот пароль в `SMTP_PASS`

#### Переменные для CORS:

```env
CORS_ORIGIN=https://ваш-домен.com,https://www.ваш-домен.com
```

Или для разработки (небезопасно для production):
```env
CORS_ORIGIN=*
```

#### Переменные для Redis (опционально):

Если вы добавили Redis плагин на Railway:
```env
REDIS_URL=redis://default:password@host:port
```

Или отключите Redis:
```env
REDIS_ENABLED=false
```

### 2.3. Настройка базы данных (SQLite) - ⚠️ ВАЖНО!

**Railway использует эфемерную файловую систему** - это означает, что все файлы удаляются при каждом редеплое. 
**Без настройки Volume все ваши данные будут потеряны!**

#### ⚠️ ОБЯЗАТЕЛЬНО: Создайте Railway Volume

**Подробная инструкция:** См. файл [RAILWAY_VOLUME_SETUP.md](./RAILWAY_VOLUME_SETUP.md)

**Краткая инструкция:**

1. В проекте Railway нажмите **"+ New"** → **"Volume"**
2. Создайте volume с именем `database-volume` (или любое другое)
3. **Подключите Volume к вашему сервису:**
   - Откройте созданный Volume
   - В разделе **"Connect"** выберите ваш сервис
   - Нажмите **"Connect"**
4. Railway автоматически создаст переменную `RAILWAY_VOLUME_MOUNT_PATH`
5. Приложение автоматически обнаружит Volume и будет использовать его для базы данных

**Проверка:**
- После редеплоя проверьте логи
- Должна быть строка: `✅ Обнаружен Railway Volume, используем путь: /mnt/volume/.../travel.db`
- Если видите `⚠️ Используется локальный путь` - Volume не подключен!

#### Альтернатива: Указать путь вручную

Если автоматическое определение не работает, укажите путь вручную:

```env
DB_PATH=/mnt/volume/ваш-volume-id/travel.db
```

**Где найти ID Volume:** В настройках Volume на Railway.

### 2.4. Настройка домена (опционально)

1. В настройках проекта Railway перейдите в **Settings** → **Domains**
2. Нажмите **"Generate Domain"** для получения бесплатного домена
3. Или добавьте свой домен, нажав **"Custom Domain"**
4. Обновите переменную `SITE_URL` с новым доменом

## 🔍 Шаг 3: Проверка деплоя

### 3.1. Проверка логов

1. В Railway перейдите в **Deployments**
2. Выберите последний деплой
3. Проверьте логи на наличие ошибок

### 3.2. Проверка работы приложения

Откройте ваш домен Railway и проверьте:

- ✅ Главная страница: `https://ваш-проект.railway.app/`
- ✅ Админ-панель: `https://ваш-проект.railway.app/admin`
  - Логин: `admin`
  - Пароль: `admin123` (⚠️ **СМЕНИТЕ ПАРОЛЬ СРАЗУ!**)
- ✅ API Health Check: `https://ваш-проект.railway.app/api/health`
- ✅ Swagger документация: `https://ваш-проект.railway.app/api-docs`

## 🔐 Шаг 4: Безопасность

### 4.1. Смена пароля администратора

1. Войдите в админ-панель
2. Перейдите в настройки профиля
3. Смените пароль на надежный

### 4.2. Проверка переменных окружения

Убедитесь, что:
- ✅ `JWT_SECRET` имеет минимум 32 символа
- ✅ `CORS_ORIGIN` не установлен в `*` (если это production)
- ✅ `SMTP_USER` и `SMTP_PASS` настроены для отправки email

## 📊 Шаг 5: Мониторинг

Railway предоставляет встроенный мониторинг:

1. **Metrics** - метрики использования ресурсов
2. **Logs** - логи приложения в реальном времени
3. **Deployments** - история деплоев

## 🔄 Обновление приложения

После каждого push в GitHub, Railway автоматически:

1. Обнаружит изменения
2. Запустит новый деплой
3. Обновит приложение без простоя

Для ручного деплоя:
1. Перейдите в **Deployments**
2. Нажмите **"Redeploy"** на нужном деплое

## 🐛 Решение проблем

### Проблема: Приложение не запускается

**Решение:**
1. Проверьте логи в Railway
2. Убедитесь, что все обязательные переменные окружения установлены
3. Проверьте, что `JWT_SECRET` имеет минимум 32 символа

### Проблема: База данных не сохраняется

**Решение:**
1. Убедитесь, что создан Railway Volume
2. Проверьте переменную `RAILWAY_VOLUME_MOUNT_PATH` в настройках
3. Или укажите `DB_PATH` вручную

### Проблема: Email не отправляется

**Решение:**
1. Проверьте настройки SMTP в переменных окружения
2. Для Gmail используйте пароль приложения, а не обычный пароль
3. Проверьте логи приложения на наличие ошибок SMTP

### Проблема: CORS ошибки

**Решение:**
1. Убедитесь, что `CORS_ORIGIN` содержит правильные домены
2. Домены должны быть разделены запятыми без пробелов
3. Используйте полные URL с протоколом (https://)

## 📚 Дополнительные ресурсы

- [Railway Documentation](https://docs.railway.app)
- [Railway Discord](https://discord.gg/railway)
- [Node.js Best Practices](https://github.com/goldbergyoni/nodebestpractices)

## ✅ Чеклист деплоя

- [ ] Репозиторий создан на GitHub
- [ ] Код загружен в GitHub
- [ ] Проект создан на Railway
- [ ] Репозиторий подключен к Railway
- [ ] Все переменные окружения настроены
- [ ] Railway Volume создан для базы данных
- [ ] Домен настроен (опционально)
- [ ] Приложение успешно запущено
- [ ] Админ-панель доступна
- [ ] Пароль администратора изменен
- [ ] Email уведомления работают
- [ ] База данных сохраняет данные

---

**Готово!** 🎉 Ваш сайт теперь работает на Railway.com!

