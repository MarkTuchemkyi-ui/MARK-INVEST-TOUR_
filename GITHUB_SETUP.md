# 📦 Настройка GitHub репозитория

Краткая инструкция по созданию и настройке репозитория на GitHub для проекта.

## 🚀 Быстрая настройка

### 1. Инициализация Git (если еще не сделано)

Откройте терминал в корне проекта и выполните:

```bash
# Проверьте, инициализирован ли уже Git
git status

# Если Git не инициализирован, выполните:
git init
```

### 2. Добавление файлов в Git

```bash
# Добавьте все файлы (кроме тех, что в .gitignore)
git add .

# Создайте первый коммит
git commit -m "Initial commit: MARK INVEST TOUR project ready for Railway deployment"
```

### 3. Создание репозитория на GitHub

1. Перейдите на [GitHub.com](https://github.com) и войдите в аккаунт
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните форму:
   - **Repository name**: `mark-invest-tour` (или другое имя)
   - **Description**: `MARK INVEST TOUR - Travel website with admin panel`
   - Выберите **Public** или **Private**
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license" (они уже есть)
4. Нажмите **"Create repository"**

### 4. Подключение локального репозитория к GitHub

После создания репозитория GitHub покажет инструкции. Выполните команды (замените `YOUR_USERNAME` и `YOUR_REPO_NAME`):

```bash
# Добавьте удаленный репозиторий
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

### 5. Проверка

Откройте ваш репозиторий на GitHub - вы должны увидеть все файлы проекта.

## 📝 Переменные окружения для Railway

После создания репозитория, при деплое на Railway вам нужно будет настроить следующие переменные окружения:

### Обязательные:
- `JWT_SECRET` - минимум 32 символа
- `NODE_ENV=production`

### Для Email:
- `SMTP_HOST=smtp.gmail.com`
- `SMTP_PORT=587`
- `SMTP_USER=ваш-email@gmail.com`
- `SMTP_PASS=пароль-приложения`
- `SITE_URL=https://ваш-проект.railway.app`

### Опциональные:
- `CORS_ORIGIN` - домены через запятую
- `REDIS_URL` - если используете Redis
- `REDIS_ENABLED=false` - чтобы отключить Redis
- `DB_PATH` - путь к БД (если не используете Railway Volume)

Подробные инструкции по деплою на Railway смотрите в файле `RAILWAY_DEPLOY.md`.

## ✅ Готово!

Теперь ваш код на GitHub и готов к деплою на Railway!

